# No log.conf here: the BunkerWeb image cannot log through a journal socket,
# so the proxy pod stays on journald. README, "Logging".
[Container]
DropCapability=ALL
NoNewPrivileges=true
PidsLimit=512
